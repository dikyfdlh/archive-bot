{
  "name": "archive-manager-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "WhatsApp archive bot with Google Drive storage.",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node scripts/check-syntax.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
